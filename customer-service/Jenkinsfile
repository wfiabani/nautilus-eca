#!/usr/bin/env groovy
pipeline{
    agent none
    stages{
        stage ('Build'){
            agent{
                docker { 
                    image 'maven' 
                    args '-u root'
                }
            }
            steps{
                sh "mvn package"
            }
        }
    }
}